# agent_dataset
using skill to analyze protocols in paper

2-stage llm extraction in ./workflow
skill in ./.agent

did not upload the paper dataset

remember to install Java JDK(opendataloader needs it, install it first)
and add your deepseek-api key
